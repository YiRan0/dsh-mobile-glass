/**
 * dsh-mobile-glass — Host half (anchor only).
 * The client half (src/client.ts → lib/client.js) carries the entire mobile UI adaptation.
 */
import type { Context } from '@deepseek-ai/cordis'

export const name = 'dsh-mobile-glass'

export function apply(_ctx: Context) {}
